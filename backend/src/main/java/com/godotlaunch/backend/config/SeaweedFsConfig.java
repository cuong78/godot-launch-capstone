package com.godotlaunch.backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for SeaweedFS storage.
 */
@Configuration
public class SeaweedFsConfig {
    // SeaweedFS runs directly via HTTP client; no complex bean initialization is required.
}
