fastapi>=0.115.0
uvicorn>=0.30.0
# ArcFace 512-d recognition + CPU inference (replaces dlib/face_recognition).
insightface==1.0.1
onnxruntime>=1.28.0
numpy>=1.26.4,<2
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
Pillow>=10.4.0
google-cloud-vision>=3.7.2
python-multipart>=0.0.9
clamd>=1.0.2
# AI review: CLIP (media↔text match) + NSFW classifier qua Hugging Face transformers.
# LƯU Ý: torch KHÔNG để ở đây — cài riêng bản CPU-only trong Dockerfile (tránh kéo ~3GB CUDA).
# Keep below 5: v5 changed CLIP get_image_features from Tensor to model output.
# Runtime code supports both shapes, while this pin prevents other major API
# changes from silently breaking AI/KYC embedding after a fresh install.
transformers>=4.44.2,<5
requests>=2.32.3
